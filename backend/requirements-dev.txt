# ============================================
# 开发依赖（测试、代码规范）
# 安装：pip install -r requirements-dev.txt
# ============================================
-r requirements.txt

# 测试
pytest>=8.0
pytest-cov>=5.0

# 代码规范（替代 flake8 + isort，一条命令搞定）
ruff>=0.5