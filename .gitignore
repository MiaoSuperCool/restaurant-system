# 根目录级忽略（backend/.gitignore 负责 backend 内部，web-staff/.gitignore 负责网页端）

# Python 虚拟环境与缓存
.venv/
__pycache__/
*.py[cod]

# IDE
.idea/
.vscode/

# 环境变量（含密钥，永不提交）
.env

# 日志
*.log

# 「本机构建好的前端产物」那条部署路径用的（见 docs/部署.md 的小内存机器那节）：
# 产物本身不该进仓库，它是构建结果，不是源码
prebuilt/
prebuilt-dists.tar.gz
